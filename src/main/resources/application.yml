ip: 10.20.131.193
server:
  port: 8080
swagger:
  enabled: true
spring:
  application:
    name: wheelcontrast
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${ip}/wheel?useUnicode=true&characterEncodeing=UTF-8&useSSL=false&serverTimezone=GMT
    username: root
    password: 123456

logging:
  level:
    sql: debug